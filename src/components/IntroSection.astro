<section class="hero-container">
  <div class="hero-content">
    <div class="hero">
      <h1>Hola, soy <span class="nombre">Juan De Dios Quiroz</span></h1>
      <p>Desarrollador Web & Especialista en Odoo</p>
      <a href="#contacto" class="btn">Contáctame</a>
    </div>

    <div class="about">
      <h2>Sobre mí</h2>
      <p>
        Soy desarrollador web con experiencia en sistemas ERP como Odoo, así como desarrollo front-end con Astro, React y tecnologías modernas.
      </p>
      <p>
        Me apasiona crear soluciones eficientes y elegantes tanto en diseño como en funcionalidad.
      </p>
    </div>
  </div>

  <div class="photo-container">
    <img src="/photo-perfil.png" alt="Foto desarrollador" class="profile-photo" />
  </div>
</section>

<style>
  .hero-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 6rem 2rem 4rem; /* Más padding arriba para el navbar */
    color: #ffffff;
    gap: 3rem;
    flex-wrap: wrap;
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
  }

  .hero-content {
    flex: 1;
    min-width: 300px;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .hero, .about {
    width: 100%;
  }

  .nombre {
    color: aqua;
    font-size: 1.25em;
  }

  .photo-container {
    width: 200px;
    height: 200px;
    min-width: 200px;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid rgba(4, 38, 75, 0.7);
    box-shadow: 0 0 20px rgba(4, 38, 75, 4);
    position: relative;
    z-index: 1;
  }

  .profile-photo {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .btn {
    margin-top: 1rem;
    padding: 0.6rem 1.4rem;
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 0.4rem;
    text-decoration: none;
    display: inline-block;
    font-weight: bold;
    transition: background 0.3s ease;
  }

  .btn:hover {
    background-color: #0056b3;
  }

  @media (max-width: 768px) {
    .hero-container {
      flex-direction: column-reverse;
      text-align: center;
      padding: 5rem 1rem 2rem; /* Ajuste para navbar móvil */
      gap: 1.5rem;
    }

    .photo-container {
      margin-top: 1rem;
    }

    .hero-content {
      align-items: center;
    }

    .btn {
      margin: 1rem auto;
    }
  }

  @media (max-width: 480px) {
    .hero-container {
      padding-top: 4rem; /* Menos espacio en móviles muy pequeños */
    }
    
    .hero h1 {
      font-size: 1.5rem;
    }
    
    .hero p, .about p {
      font-size: 0.9rem;
    }
    
    .photo-container {
      width: 150px;
      height: 150px;
      min-width: 150px;
    }
  }
</style>